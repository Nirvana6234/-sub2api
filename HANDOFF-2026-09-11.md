# 交接说明 — 2026-09-11

写给接手的下一个 AI。本文档由上一个 session（Claude）在被用户叫停后整理，目的是把这几天（本次多 session 接力）做的事、参照的依据、目前的状态、以及用户提出的疑虑都讲清楚，避免重新摸索浪费时间，也避免带着错误假设继续往下改。

## 0. 用户叫停的原话

> 你现在改了很多功能啊，我以前的功能都被你改过头了（https://github.com/Nirvana6234/-sub2api），你对照着我的这个，写一个解释说明，把来龙去脉都写清楚，我将派另一个AI来接手解决这个问题。你可以收手了。

**这句话里最关键的信息**：用户认为当前分支（`local/main`）相对于 `https://github.com/Nirvana6234/-sub2api` 这个仓库，被"改过头"了——即存在不该改的改动，或者遗漏/走样了该保留的东西。下一个 AI 的首要任务应该是**认真对照这个仓库做一次全面 diff review**，而不是想当然地信任这份交接文档里的判断。

## 1. 任务的背景是什么

这是一个跨多个 session 的长期任务。项目根目录 `AGENTS.md`（`sub2api云服务/枫迹云-154.9.26.202/AGENTS.md`）是 Claude Code 和 Codex 共用的仓库地图，务必先读。

`sub2api/` 目录本身是一个 git 仓库，当前所在分支是 `local/main`。这个仓库配置了两个远程：

```
nirvana   https://github.com/Nirvana6234/-sub2api.git   (就是用户这次提到的仓库)
upstream  https://github.com/Wei-Shaw/sub2api.git        (公开上游)
```

`local/main` 和 `nirvana/main` **没有共同祖先**（历史上是各自独立发展出来的），所以不能直接 `git merge` 或 `cherry-pick`，只能靠人工对照 diff、逐块决定"这段是不是该往 local/main 上搬"。

任务的核心指令（来自更早的 session，一直延续到现在）：**把 `local/main` 补齐到跟"生产环境实际在跑的东西"功能对等**，做法是拿 `nirvana/main` 当参照物，把生产已经上线、但 `local/main` 还没有的功能一项项搬过来、合并进去，最终目标是把所有分支收拢成一个干净的主分支。

**用户在更早的 session 里给过一条非常关键的纠偏指令，本 session 也一直在遵守**：

> 我不明白你干这个事情很难吗？原来本来这些功能就是在这个项目里面的，只不过你要你去把它重新合并一下，抄过来。你为什么老是要自己写呢？那你要自己写，你也可以，但是你得先让我知道为什么

意思是：**优先用 `git show nirvana/main:<path> > <local_path>` 整文件复制，或者 `diff` + `patch` 做精确合并，而不是自己手打/重新实现**。本 session 一直在按这个原则做事。

## 2. 一个刚发现、可能很关键的事实：本地 `nirvana/main` 是过期快照

在被用户叫停前，我顺手做了一次 `git fetch nirvana --dry-run`，结果是：

```
6d1faa0c5..755937dcc  main  ->  nirvana/main
```

也就是说，**本 session 全程参照的 `nirvana/main`（commit `6d1faa0c5`，抓取于 2026-09-10 00:16:32）已经落后于 GitHub 上 `Nirvana6234/-sub2api` 仓库的真实最新状态（`755937dcc`）**。这个 `fetch` 是 dry-run，**没有真的拉取新提交，本地 `nirvana/main` 还停在旧的那个点**。

**这很可能是用户说"改过头了"的部分原因**——如果 GitHub 上的 `main` 在这期间又有新提交（可能是修复、可能是功能调整、也可能是撤销了某些东西），而本 session 一直在对照一个滞后的快照工作，那么：

- 有些我判断"是独立的、不相关的功能，不该搬"的东西，可能在最新版里已经被官方自己合并/调整过了；
- 有些我搬过来的东西，可能在最新版里已经有了更新/修复版本，我搬的是旧版；
- 具体差异是什么，**没有查证，需要下一个 AI 先 `git fetch nirvana` 拉到最新，再重新审视本 session 的判断**。

## 3. 本 session（以及更早几个 session）完整做了什么

### 3.1 更早 session 已完成并验证过、已提交的四个功能（这些应该没问题，但既然用户说"以前的功能也被改过头"，也建议下一个 AI 抽查一下是否跟最新 GitHub 版一致）

```
f5fe4b058 feat(latency-compensation): 移植生产已上线的延迟补偿功能
278be0dc3 feat(headroom): 移植生产已上线的上下文压缩(headroom)功能
90d2b865b feat(fallback-pool): 移植生产已上线的复数兜底池 fallback_group_ids
04e736e5b fix(admin-group): 编辑分组对话框静默清空兜底配置的生产事故
```

这四个都各自验证过 `go build`/`go vet`/`go test` 全绿，前端 `vue-tsc`/`vitest` 全绿。

### 3.2 本 session 完成的：贡献房间（Contribution Rooms）功能

这是本 session 的主线任务，backend 和 frontend 都已经写完并且做过验证（细节见下），但**全部是未提交的工作区改动**，还没有 commit。

**backend 部分**：
- ent schema 四张新表（`contribution_rooms`、`contribution_room_accounts`、`contribution_account_verifications`、`user_contribution_room_preferences`）+ `proxies.owner_user_id`、`groups.allow_contribution_pool`、`users.account_management_enabled`/`contribution_rooms_enabled` 字段
- 迁移文件 `backend/migrations/249_contribution_rooms.sql`、`250_contribution_room_support_columns.sql`、`251_contribution_wallet_settlements.sql`——**注意编号是全新的 249-251，不是照抄 nirvana 那边分散的 176-186/199 号迁移**，原因见下面第 5 节的重要提醒
- 一批新文件：`account_contribution_handler.go`、`account_contribution_anthropic_handler.go`、`account_contribution_openai_handler.go`、`contribution_room_handler.go`、`contribution_proxy_handler.go`、`admin/account_contribution_governance.go`、`admin/account_contribution_guard.go`、`admin/account_test_models.go`、`contribution_room_routing.go`、`contribution_room_routing_repo.go`、`openai_test_model.go`、`middleware/user_feature.go`
- 计费热路径改动：`gateway_usage_billing.go`、`usage_billing.go`、`usage_billing_repo.go`（贡献房间/公共池分成结算逻辑）
- 调度改动：`gateway_scheduling.go`、`openai_gateway_scheduling.go`（把贡献房间路由接进候选账号列表；**特别小心跳过了** Workspace 桌面客户端本地路由、Grok 粘性会话、粘性会话失败自愈、以及一处会砍掉 Kimi/智谱/深度求索/MiniMax 支持的平台白名单倒退——这几个是跟贡献房间绑在同一个 diff 里的**不相关功能**，判断依据见第 6 节）
- Wire 依赖注入：`cmd/server/wire.go`、`wire_gen.go`、`internal/service/wire.go`、`internal/repository/wire.go`、`internal/handler/wire.go`
- 两处**顺手发现并修复的真实 bug**（不是贡献房间本身的功能，是这轮操作过程中暴露出来的）：
  1. 管理员账号列表本来会把用户贡献的私有账号也列出来（`account_repo.go` 的 `accountListFilteredQuery` 之前没接上早就声明好但从未使用的 `ctxkey.AdminAccountManagementList`/`AllowContributionAccountManagement`）
  2. 房间账号本应被排除出普通分组调度候选列表（避免绕过房间预算/并发限制），`account_repo.go` 的 `queryAccountsByGroup` 之前没加这个排除
  3. Wire 重新生成时差点冲掉一段手改的 Paw 聊天/Playground 历史记录接线（`ProvidePawConfigService`/`ProvidePawChatService`/`NewPlaygroundHistoryService` 等，已经补成正规 Wire provider 声明）

**frontend 部分**：新增 `views/admin/ContributionRoomsView.vue`、`views/admin/ContributionsView.vue`、`views/user/AccountContributionsView.vue`、`views/user/SharedContributionRoomsView.vue`、`components/account/ContributionTestModal.vue`、`api/contributionRooms.ts`、`api/admin/contributionRooms.ts`、`api/admin/contributions.ts`、`api/accountContributions.ts`、`utils/proxyAddress.ts` + 对应测试文件；修改了路由（`router/index.ts`、`meta.d.ts`）、侧边栏导航（`AppSidebar.vue`）、i18n（en/zh 的 `common.ts`、`dashboard.ts`、`admin/index.ts`、`admin/overview.ts`、新增 `admin/contributions.ts`）、`types/index.ts`、用户创建/编辑弹窗（`UserCreateModal.vue`/`UserEditModal.vue` 加了"允许用户账号管理"/"允许用户贡献房间"两个开关）。

**验证情况**：
- `go build ./...`、`go vet -tags unit ./...` 全绿
- `go test -tags unit ./internal/repository/... ./internal/service/...` 除 4 个**已确认跟本次改动无关的历史遗留失败**（见下）外全绿
- 前端 `vue-tsc --noEmit` exit 0，`vite build` 成功，`vitest run` 291 个测试文件 / 2154 个用例全部通过
- **在日本服务器上做了一次完整的真实环境验证**（见第 4 节）：全新数据库、251 个迁移从零跑通、后台管理页面和用户页面都用真实浏览器点过，功能正常

### 3.3 本 session 中途开始、**被打断、只做了一半**的："共飞 AI" 品牌还原

用户在部署完贡献房间之后发现新部署的实例是 Sub2API 原版皮肤（青绿色配色、"Sub2API"站名、原版 logo），不是用户自己的"共飞 AI"品牌（天空蓝配色、飞机 logo、"共飞 AI"站名、还有一个"共飞 AI 助手"桌面客户端下载入口）。

对照 `nirvana/main` 发现，"共飞 AI"品牌是一套**很大范围的改动**，横跨 30+ 个文件：后端有硬编码兜底字符串（邮件模板里的站名兜底值、TOTP 二次验证的 issuer 名）、一条把默认 `site_name`/`site_subtitle` 改成"共飞 AI"/"一起共享，一起使用 AI"的迁移、一整套新的"共飞 AI 助手"桌面客户端下载设置（`chat_app_download_enabled`/`chat_app_download_direct_url`/`chat_app_latest_version`，独立于已有的 Sub2API 客户端下载）；前端有 `tailwind.config.js` 的完整配色/字体替换、新的 logo 素材（`gongfei-plane.svg`）、登录页（`AuthLayout.vue`）的飞机动画视觉重做、`index.html`、`router/title.ts`、`stores/app.ts` 里的站名兜底值、以及 `ClientDownloadView.vue` 需要加上"共飞 AI 助手"下载区块。

**已经做完的部分**（按 git-copy-first 原则，只搬了跟品牌直接相关的字符串/资源，跳过了跟品牌无关但混在同一个 diff 里的东西，比如 `embed_on.go` 那一大坨完全独立的 SEO/robots.txt/sitemap.xml/JSON-LD 功能，那个只是顺带引用了 `gongfei-plane.svg` 文件名，跟品牌本身无关）：

- `backend/internal/service/totp_service.go`：`totpIssuer` 改成"共飞 AI"
- `backend/internal/service/content_moderation.go`：两处 `siteName()` 兜底值改成"共飞 AI"
- `backend/internal/service/auth_service.go`（3 处）、`auth_email_binding.go`、`auth_oauth_email_flow.go`、`balance_notify_service.go`、`user_service.go`：邮件相关的 `siteName`/`defaultSiteName` 兜底值改成"共飞 AI"
- `backend/internal/service/domain_constants.go`：新增 `SettingKeyChatAppDownloadEnabled`/`SettingKeyChatAppDownloadDirectURL`/`SettingKeyChatAppLatestVersion` 三个常量
- `backend/internal/service/setting_parse.go`：加了这三个新 key 的默认值
- `backend/internal/service/settings_view.go`：两处 struct 加了这三个字段（**已完成到这一步时被打断**）
- `frontend/public/gongfei-plane.svg`：已经从 nirvana 复制过来了

**还没做、需要下一个 AI 接手的部分**（按原计划顺序）：

1. `backend/internal/service/setting_update.go`：加 `updates[SettingKeyChatAppDownloadEnabled] = ...` 这三行（照抄同文件里 `ClientDownloadEnabled` 那几行的写法）——**注意**：查过 `nirvana/main` 的 `setting_update.go`，这三个 key 在那边**居然也没接进管理员批量保存的路径**（可能是 nirvana 自己也没做完/是个遗留 bug），所以这里我是照着已有的 `ClientDownloadEnabled` 模式**主动补全**的，不是照抄 nirvana——如果下一个 AI 重新核对最新 GitHub 版本发现这里已经修了，以那边为准
2. `backend/internal/service/setting_public.go`：公开设置里也要加这三个字段（参考 `ClientDownloadEnabled` 的写法，一共大概 4 处：`PublicSettings` struct 定义、GetPublicSettings 组装、白名单 key 列表）
3. `backend/internal/handler/dto/settings.go`：两处 struct 加这三个字段（管理员设置的 GET/PUT DTO）
4. `frontend/tailwind.config.js`：应用完整的 diff（主色调从 teal/cyan 换成天空蓝、字体加 HarmonyOS Sans SC/Alibaba PuHuiTi、阴影/渐变颜色跟着换）
5. `frontend/index.html`：favicon 换成 `/gongfei-plane.svg`（**只改这一行**，不要把同一个 diff 里那堆 SEO meta 标签也搬过来，那是不相关的 SEO 功能）
6. `frontend/src/router/title.ts`：默认站名兜底值 `'Sub2API'` → `'共飞 AI'`
7. `frontend/src/stores/app.ts`：两处默认站名兜底值改成"共飞 AI"（**注意**：同一个 diff 里还带了 `chat_app_download_enabled`/`chat_app_download_direct_url`/`chat_app_latest_version` 三个字段要加到 `cachedPublicSettings` 的默认结构里，这个要加，是新客户端下载功能的一部分，不是无关内容）
8. `frontend/src/components/layout/AuthLayout.vue`：整个文件按 nirvana 版本重做（飞机动画、虚线飞行轨迹装饰、配色，是独立自包含的一个文件，可以整体套用）
9. `frontend/src/components/layout/AppSidebar.vue`：logo 兜底改成 `/gongfei-plane.svg`，alt 文本改成 `${siteName} 标志`——**这个文件本 session 已经改过（贡献房间导航项那次），改品牌相关的这几行时要小心合并，不要覆盖掉已经加进去的"共享账号治理"/"贡献房间"/"账号管理"/"共享房间"导航项**
10. `frontend/src/views/ClientDownloadView.vue`：diff 78 行，加"共飞 AI 助手"下载区块（这是用户在本轮明确追问"还有我的客户端下载界面呢"提到的），需要读一下 diff 具体是怎么加的，可能还要看对应的管理员设置页（`views/admin/SettingsView.vue`）有没有配套的配置入口要加
11. 新迁移文件（比如 `252_rebrand_default_site_identity.sql`）：把 nirvana 那条 `UPDATE settings SET value='共飞 AI' WHERE key='site_name' AND value='Sub2API'` 迁移抄过来（注意 `WHERE value = 旧默认值` 这个条件，是为了不覆盖管理员已经自定义过的站名，这个保护逻辑要保留）
12. 还有 grep 里提到但没细看的几个文件：`frontend/src/i18n/locales/zh/landing.ts`、`zh/misc.ts`、`frontend/src/views/HomeView.vue`、`frontend/src/views/KeyUsageView.vue`、`frontend/src/views/admin/SettingsView.vue`、`frontend/src/views/public/LegalDocumentView.vue`、`backend/internal/server/middleware/api_key_auth_workspace_test.go`、`frontend/src/api/keys.ts`——这几个都还没打开看过 diff，不确定是纯品牌还是又混了别的功能，下一个 AI 需要重新过一遍，别假设

## 4. 日本服务器（18.182.62.112）现在是什么状态

用户在对话中途要求把当前代码部署到这台服务器（"这是我的日本服务器"），过程和现状如下：

- SSH 密钥在 `vpn-manager/keys/18.182.62.112-18.182.62.112-dldzyjs.pem`，登录用户是 `ubuntu`（不是 `ec2-user`）
- 这台服务器**之前就已经在跑 sub2api**，容器名 `sub2api`（镜像 `sub2api:0.1.171-playground-current-group-r1`），代码在 `/home/ubuntu/web_transform/sub2api`，**这个 checkout 的 git remote 是 `git@github.com:Liuna-llf/web_transform.git`——跟本机用的 `Wei-Shaw/sub2api`/`Nirvana6234/-sub2api` 都不是同一个仓库**，看提交信息（"Ship user account contribution support to deployment source"、"Prepare private deployment source for cloud build"）像是专门为云端构建整理出来的"部署源"分支
- 这台服务器数据库里**已经有贡献房间功能了**，但迁移编号是 176-186、199（跟这台机器自己的 `schema_migrations` 记录一致，最新到 `200_cleanup_stale_auto_group_candidates.sql`），**和本 session 新写的 249-251 编号完全不是一回事**——这两套贡献房间的表结构是否字段级别一致，**没有比对过**
- 用户明确说这台服务器"已经闲置"，让我当全新服务器随便处理，用来验证 `local/main` 当前代码能否正常跑
- 我在本地（Windows）交叉编译了 `backend/bin/sub2api-linux-amd64`（linux/amd64，`-tags embed`，前端一起打进二进制），没有在云端跑任何 npm/go 编译，用仓库里现成的 `deploy/Dockerfile.binary` 模式打了一个很小的运行时镜像（只有拷贝文件 + apk 装几个包，25 秒完事）
- 新建了一个空数据库 `sub2api_verify`，251 个迁移从零跑通，无一失败
- **已经把这个新镜像正式切到 8080 端口**，替换了原来的旧容器（旧容器改名 `sub2api-old-0.1.171-20260911T005147` 保留着、镜像和数据都没删，回滚只需要 `docker start` 它、把新容器停掉/改名让端口）
- 管理员账号是这次验证流程里建的临时账号：`verify@sub2api.local` / `VerifyLocal2026Test`，**强烈建议登进去改成正式邮箱密码**
- 访问方式：服务器上 nginx 配了个反代规则把域名 `api.guinian--ai.cc.cd` 转发到本机 8080，**但这个域名目前 DNS 解析指向的是另一台服务器 `154.9.26.202`，不是这台日本机器**，所以现在实际能用的访问方式是**直接用公网 IP、不带端口号**：`http://18.182.62.112`（8080 端口对公网没开放，安全组应该只放行了 80/443/22，本机没有 AWS API 凭据查不到/改不了安全组规则）
- **用户看到这个部署后提出了本文档开头那个问题**：为什么皮肤不对、游乐场呢、兜底池是不是还能用——追问过程中我确认了 Playground 功能默认是关闭的（`SettingKeyPlaygroundEnabled` 默认值 `"false"`，纯粹是设置开关问题，不是没做），兜底池调度部分本 session 没有改动核心逻辑（只是在候选列表构建里叠加了贡献房间路由，是 additive 的，理论上不影响兜底池，但**没有针对兜底池单独在这台服务器上跑一遍端到端验证**，下一个 AI 如果要继续这台服务器的验证，这块建议补一下

## 5. 关于迁移编号 249-251 的选择，为什么不是照抄 nirvana 的 176-186/199

早前的 session 判断：`nirvana/main` 的贡献房间相关迁移分散在 176 到 199 号之间（而且和 `local/main` 自己已有的、同一数字区间的其他迁移文件**编号冲突**——两边独立发展出的迁移历史，同一个数字对应的是完全不同的迁移内容），与其在 `local/main` 里硬塞进去、手动重新排号，不如写**一份内容上完全等价、但编号是 local/main 自己下一个可用号**的干净迁移（249-251），这样两边内容不会打架。

**这个决定现在看来可能是导致"跟 nirvana 对不上"疑虑的原因之一**——文件名和编号不一样，直接 diff 两个仓库的 `backend/migrations/` 目录会显得差异很大，但内容层面的表结构理论上应该是等价的（没有逐字段核对过，见第 4 节的提醒）。下一个 AI 如果要验证这个决定是否合理，可以重点比对：`nirvana/main` 里 176/177/178/...199 那些跟贡献房间相关的迁移文件的最终表结构，和本地 249/250/251 的表结构，是否字段、约束、索引都一致。

## 6. 本 session 明确判断"不相关、不搬"的功能，附判断依据

这几个是在对照 `nirvana/main` 的过程中，因为跟贡献房间/品牌捆绑在同一个 diff 块里出现，但判断为独立功能而**没有**移植的东西。列出来是为了让下一个 AI 知道这是"看过、判断过、主动跳过"，而不是"没看到"：

- **Workspace 桌面客户端本地路由**（`prependWorkspaceOwnedAccounts`、`compareWorkspaceOwnedOpenAIAccounts`、`IsWorkspaceLocalFallbackRoute`）：让桌面客户端用户的私人账号优先于兜底账号，是一个完全独立的调度策略功能，出现在 `gateway_scheduling.go`/`openai_gateway_scheduling.go` 的贡献房间 diff 里，但跟贡献房间本身无关
- **Grok 粘性会话**（`grokPreviousResponseSessionSeed` 相关）、**粘性会话失败自愈**（连续失败自动解绑）：混在同一批调度文件改动里，跟贡献房间无关
- **一处平台白名单倒退**：`NormalizeOpenAICompatiblePlatform`（导出）被改造成 `normalizeOpenAICompatiblePlatform`（未导出），且平台白名单从 `Grok/Kimi/Zhipu/Deepseek/MiniMax` 缩水成只剩 `Grok`——如果照抄，Kimi/智谱/深度求索/MiniMax 这几个我们现在支持的平台会直接失效，**明确没有采纳**
- **`SchedulabilitySource`/`SchedulabilityReason`/`SchedulabilityChangedAt`**：账号审计轨迹功能，是给 TransitHub 消费的，跟贡献房间无关（这个是更早 session 就已经识别过并排除的，本 session 延续了这个判断）
- **`RegistrationIPQuotaRepository`**：IP 注册限额功能，跟贡献房间无关
- **工单（Ticket）系统**：贡献房间的 diff 里混进了一整套工单管理（`ticket_repo.go`、`registerTicketRoutes`、侧边栏"工单"入口等），是完全独立的一个大功能，**没有移植**，如果用户需要这个功能，需要单独立项处理
- **`embed_on.go` 里的 SEO/robots.txt/sitemap.xml/Open Graph/JSON-LD 系统**：品牌相关 diff 里混进来的，300+ 行的独立功能，只是顺带引用了品牌 logo 文件名，**没有移植**

## 7. 本 session 顺手发现、已经拆成独立任务、还没人处理的历史遗留问题

这几个是全量跑测试时顺手发现的、**跟贡献房间/品牌都无关的历史 bug**，已经通过 `spawn_task` 拆成独立任务卡片（用户点一下就能开一个新 session 去修），如果这些卡片还在、还没处理，下一个 AI 可以直接认领：

1. `TestAdminServiceSimpleModeNormalizesAllUnsupportedUpdateFieldsDirectly`、`TestGetUserGroupVisibilityIncludesActiveSubscriptions`、`TestGetUserGroupVisibilityEmptyAndErrors` 三个测试稳定失败——兜底组循环检测 + 分组可见性两处疑似真实 bug
2. `usage_logs` 表 INSERT 语句占位符个数和实际传参个数对不上（67 vs 69）——**这个如果是真的，线上每条 usage log 写入都可能在某些路径下失败**，优先级较高
3. `TestAPIContracts` 黄金快照严重过期（缺了近几轮 headroom/兜底池/延迟补偿/贡献房间陆续加的字段）+ 一处余额耗尽应返回 403 却返回 200 的中间件测试失败

（这两个任务在对话过程中我看到用户已经在别的 session 里点开跑过了，可能已经有进展或已经修完，下一个 AI 如果要看进度，去问用户或者直接 `git log`/`git diff` 相关文件确认。）

## 8. 给下一个 AI 的建议

1. **第一步不要相信这份文档的任何判断，先自己 `git fetch nirvana` 拉到最新，然后认真对比 `local/main` 和最新 `nirvana/main` 的实际差异**——本文档第 2 节已经确认本 session 参照的版本是过期的，具体差多少、差在哪，需要重新核实。
2. 品牌还原按第 3.3 节的清单接着做，注意合并 `AppSidebar.vue` 时不要覆盖已有的贡献房间导航项。
3. 贡献房间功能本身（backend + frontend）已经做完、测试全绿，**但没有 commit**，也没有推送到任何远程。148+164 个改动都在工作区里，建议先跟用户确认要不要先 commit 一次"结账点"，再继续改品牌，免得改出问题时不好回退。
4. 日本服务器那边如果要继续验证，记得先把 8080 的临时管理员账号改掉，且兜底池功能建议单独跑一次端到端验证（本 session 没来得及做）。
5. 涉及生产/半生产环境的操作（数据库迁移、容器替换）务必谨慎，做之前先跟用户确认范围，参考 `deploy/DEPLOY-PRODUCTION.md`（虽然是写给另一台服务器 `52.76.22.21` 的，但操作纪律可以参考）。
