{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "共飞 AI 工作台主窗口的能力集。前端要调 agent_* 那组命令（应用自定义命令，不走 ACL），以及选工作目录用的对话框。**新增能力必须显式加在这里**，每加一条就是多给前端一份原生权限。",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
