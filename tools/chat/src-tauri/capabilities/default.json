{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "共飞 AI 工作台主窗口的默认能力集。前端目前不调用任何 Tauri API，故只保留 core:default。新增能力必须显式加在这里。",
  "windows": ["main"],
  "permissions": ["core:default"]
}
