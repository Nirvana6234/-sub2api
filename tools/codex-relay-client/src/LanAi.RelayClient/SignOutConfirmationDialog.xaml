<Window x:Class="LanAi.RelayClient.SignOutConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="退出账号"
        Width="430"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="#FFFFFF">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="要退出当前账号吗？"
                   FontSize="18"
                   FontWeight="SemiBold" />

        <TextBlock x:Name="ConsequenceText"
                   Grid.Row="1"
                   Margin="0,10,0,22"
                   Foreground="#4B5563"
                   TextWrapping="Wrap" />

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Content="确认退出账号"
                    Click="SignOut_OnClick"
                    Padding="12,9"
                    Background="#FFFFFF"
                    BorderBrush="#D1D5DB"
                    Foreground="#B91C1C" />

            <Button Grid.Column="2"
                    Content="最小化到托盘"
                    Click="Minimize_OnClick"
                    Padding="12,9"
                    Background="#2563EB"
                    BorderBrush="#2563EB"
                    Foreground="#FFFFFF"
                    FontWeight="SemiBold"
                    IsDefault="True" />
        </Grid>
    </Grid>
</Window>
